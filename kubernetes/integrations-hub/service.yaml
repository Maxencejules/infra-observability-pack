apiVersion: v1
kind: Service
metadata:
  name: integrations-hub
  namespace: portfolio
  labels:
    app: integrations-hub
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: integrations-hub
