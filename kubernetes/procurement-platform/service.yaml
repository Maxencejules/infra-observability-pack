apiVersion: v1
kind: Service
metadata:
  name: procurement-platform
  namespace: portfolio
  labels:
    app: procurement-platform
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: procurement-platform
